---
#- hosts: all
##  vars_files:
##    - env_variables
##  remote_user: cmpdevops
##  gather_facts: false
##  tasks:
##   - name: Disabling swap on all nodes
##      shell: date
- name: Disabling Swap on all nodes
  #vars_files: 
   # - env_variables
  shell: swapoff -a

- name: Commenting Swap entries in /etc/fstab
  replace:
    path: /etc/fstab
    regexp: '(.*swap*)'
    replace: '#\1'
